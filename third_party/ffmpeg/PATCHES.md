# FFmpeg patch record

Forge `v0.2.0-cli.1` builds the unmodified FFmpeg 8.1.2 release source. No downstream
patches are applied. The exact configure flags and source URL are recorded in
`BUILD.md`; the release archive also includes `ffmpeg-version.txt` and the matching
FFmpeg source archive.
