# 三方向角色候选人工审核通过

用户在本任务中明确表示「人工审核角色后选通过」，据上下文记录为当前角色候选的
人工视觉审核通过。审核对象是此前为用户打开的 `directional-reuse-v1-20260907`
三方向 Godot 候选，完整决定与文件 SHA-256 见 [审核记录](artifacts/forge-directional-human-review-2026-09-07/approval.json)。

记录绑定当前项目配置、脚本、运行合同、52 张源帧的顺序及原生时长、13 张安装纹理、
7 个文本资源和当前通过的 Godot 运行报告。本次重新逐帧裁切 atlas，52 个区域的 RGBA
均与源 PNG 完全一致。审核记录在本地候选根目录也保存为 `human-review-20260907.json`。

Godot 编辑器和后续运行曾更新项目配置与运行报告，因此旧 verification 中对应的两个
哈希属于历史快照。本次追加记录绑定当前文件，不改写旧报告、旧截图或旧 ZIP。
`merge-candidate-v1` 与审核对象使用同一来源与运行合同，是独立 main 验证的工程副本，
没有把它称为被用户单独看过的候选。

此次决定批准该运行时候选的视觉效果。up 方向原始尺度门禁的历史失败结论保留；
没有自动晋升生产资产、替换源 Pack、添加左向或 idle，也没有调用生成 Provider。
生成时合同及历史工程报告中的 `pending` 描述当时状态，后续人工决定以本审核记录为准。

如候选的帧、时长、运行合同或视觉输入文件发生变化，本记录不能自动批准修改后的版本。
