# Local ComfyUI image P0 validation — 2026-09-23

Scope: a developer build on Windows `x86_64-pc-windows-gnu`, Qwen Image 2.1 in the user's local ComfyUI 0.37.0, and Godot 4.6.3. This is source-tree QA, not a packaged release or a three-agent natural-language discovery result. The installed ComfyUI workflow and model weights remain on the user's machine; no generated media is committed.

The source branch was `codex/comfyui-asset-create`, based on `85e3c68` (PR A). The API-format workflow was exported from the installed Qwen UI workflow, with explicit prompt node `459:452` (`TextEncodeQwenImage21`) and output node `461` (`SaveImageAdvanced`). Its SHA-256 was `4bbc89aacfda7efa46ddf9abb06ad3d88f15c378b8f773dfde4c504f7ddcc6b1`; the declared model was `qwen_image_2.1_int8_convrot.safetensors`. The development build had no optional features. Rebuilt binaries will have a different SHA; the build used for the live run was `f1829f846dbbbb3971c9806b3b4e988daed5e742fff5958e9ead7306ab99c51e`.

`forge provider doctor` validated the imported profile against the running service. `forge asset create --wait --json` submitted exactly one prompt (`8834725b-a9b2-41ae-8c46-f844a090aae6`) for parent Job `22dbdfb8-4916-4d36-bdda-4bbedb8e4dcd`. The returned 1024×1024 RGBA PNG was retained with SHA-256 `e71f89a1e1e4a16826e1786deb11a73f3565435ff36a0993443e2383aba59f7a`. It contained opaque, partial-alpha, and transparent pixels. The first preparation used foreground alpha threshold 1 and produced an unusably small icon in the contact sheet. A review bound to that source hash rejected the Job; it created no Godot install Job.

A new request reused the **same retained source** with threshold 32. Its contact sheet showed a usable blue potion icon. In an isolated Godot project, a separate parent Job `ff2374fe-b3b7-4642-89e7-eb8fe5c9d499` recorded a source-hash-bound agent visual review, installed the Pack through child Job `71aa1902-83f7-4cd2-bfa8-ea44ab669daf`, and passed Pack validation. Godot headless loaded `res://addons/forge_assets/blue_potion/items/blue_potion.png` as `Texture2D`, 128×128 pixels. This is an agent QA review in a test project, not human approval for a shipped game.

Focused verification: `cargo test --locked -p providers --test comfyui_contract`; `cargo test --locked -p forge-cli --test local_asset_create`, including the ignored native-Godot case with `GODOT_BIN`; `cargo clippy --locked -p forge-cli -p providers --no-default-features -- -D warnings`; and `python scripts/test-cli-skill.py` against the absolute development executable path (164 commands, including the Claude Code project-skill installation). Synthetic tests cover exact prompt/history/output handling, retained-source review, recovery without resubmission, and Godot installation. Three separate agents calling the CLI from natural language, a packaged CLI, macOS, and H3 video remain unverified.
