# Vidu Q2 12:23 authoritative walk-right prompt

The following prompt is the durable, byte-hash-bound prompt associated with
Vidu task `3428787657221047` and source video SHA-256
`123d82fe5088a86083ab2dd689c9c5b9388181700341bb9ee09f666341007d38`.
The canonical prompt file SHA-256 is
`cf110e6e8bd503200a8053421ca9f37132e2a5a2b0887600a4140a7b168a8861`.

> 使用首帧作为唯一角色身份、服装、比例、线稿、色板、朝向屏幕右侧和纯洋红背景的权威参考。生成一段 5 秒、1:1、无音频、固定镜头的 2D 游戏角色普通走路动作母片。角色始终原地朝右，连续重复完整步态：接触A（左脚自然向前接触、右脚在后）→过脚A（右脚低高度经过支撑脚）→接触B（右脚自然向前接触、左脚在后）→过脚B（左脚低高度经过支撑脚）→回到接触A。至少完成三个清晰的 A/B 交替周期，左右靴必须明确交换前后位置；髋部、大腿、膝盖和小腿必须随步态一起自然摆动，不能只移动脚尖或让脚在地面滑动。脚抬高度不超过靴子高度的四分之一，不跑步、不高抬腿。
>
> 骨盆/root 固定在同一屏幕坐标，不横向移动，不转身；双脚使用同一地面基线。上半身、脸、头发、兜帽、斗篷轮廓、腰带、护腕、服装颜色和线条在所有帧中保持一致，不重绘、不闪烁、不变色、不变形，手臂和斗篷只允许极轻微自然摆动。末帧回到与首帧相同的中立接触姿势。每一帧严格保持均匀纯洋红色 #FF00FF 背景，不生成地面、阴影、环境、道具、文字、标志、水印、特效、镜头移动、缩放、剪辑或转场。

The prompt is retained verbatim in the canonical file above. Future attempts
must create a new prompt record rather than overwrite this one.
