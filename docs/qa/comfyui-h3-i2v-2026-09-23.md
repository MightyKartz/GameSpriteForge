# Local H3 I2V P1 validation — 2026-09-23

This source-tree test used the user's local ComfyUI 0.37.0 on Windows, a 16 GB RTX 4090 Laptop GPU, Forge development build `x86_64-pc-windows-gnu` with no optional features, and Godot 4.6.3. The Forge binary used for the H3 runs had SHA-256 `28e66d7ead097c510cdfa81b021e1fd0651bb62e43cca145810547979fe06062`. The source branch was `codex/comfyui-video-delivery` (stacked on PR B); the binary was built from an uncommitted development state and is not a release package. No model weights or generated MP4 are committed.

The installed UI workflow was exported to API format and adapted with an explicit `LoadImage` first-frame node. The API JSON SHA-256 was `2aa394bfd9712a8b62201272f5b5fdc84a9ff9f7abd354187d3e4f9ec6dac190`. Its prompt mapping is node `6`/`prompt`, reference mapping node `20`/`image`, and result node `15` (`SaveVideo`). The model identifier declared by the user profile was `minimax_h3_fl2va_pruned_w4a8_mixed.safetensors`. This installation returned `MiniMax_H3_Turbo8_*.mp4` under output field **`images`**, not `videos`. `provider doctor` checked the live node classes and both input mappings; only actual generation confirmed the result field.

Two I2V runs used an existing robot character frame. The first, outside Forge, yielded an MP4 with H.264 video (512×288, 24 fps, 56 frames, 2.333 s) and AAC audio. Forge imported it, retained its bytes, produced a character Pack and preview, then recorded an explicit rejected review because the purported walk remained close to a T pose. Parent Job `88187604-ab22-4ac7-8ec1-3d0433c6f848` had no installation Job. The second run was submitted by `forge asset create` using profile `h3-i2v-v2`. Parent Job `558d3ae3-0bcb-426a-b79a-57d610a359cf` retained prompt `c9b3cd79-7ffa-4d67-9cb1-3b8bbfa2f769` across an asynchronous `--resume`; it uploaded the reference, fetched the exact prompt's MP4, probed it, sampled 12 frames and produced a Pack/GIF preview. Source SHA-256: `b09549bff50a05aa6acbb56628fa392a7730a522ce9f1c20119d9f68103d5af2`.

The second clip showed subtle visor/body motion and a stable silhouette. Its `idle_breathe` technical loop match was 0.946 and its quality report said `game_ready` while explicitly leaving `visualApproval` as `not_assessed`. An agent visual QA review approved it **only as a subtle idle** for an isolated test project. A new source-import request reused the exact MP4 without another H3 generation, froze the Godot target, and recorded the hash-bound review. Parent Job `059aed12-085b-48e4-940c-bbab74acfb20` installed through Job `10714200-54c0-4815-8c46-cd3c20382797`. Godot headless loaded `forge_animated_sprite.tscn`, found 12 `idle_breathe` frames, verified looping and first/last textures, and started playback: `PASS H3 Forge Godot animation: idle_breathe 12 frames`. The original MP4 keeps its AAC track; the Godot character Pack contains sampled frames, with no audio asset.

This is development evidence, not human artistic acceptance of a shippable character. The supporting `idle` animation in the isolated pack is a static two-frame hold from an existing PNG. Three-agent natural-language discovery, macOS/package verification, workflow upgrades, and paired direct-agent comparisons remain separate acceptance work.
