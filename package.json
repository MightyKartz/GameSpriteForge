{
  "name": "game-sprite-forge",
  "private": true,
  "version": "0.2.0-cli.1",
  "type": "module",
  "scripts": {
    "build": "cargo build -p forge-cli --release",
    "test": "cargo test",
    "build:automation": "cargo build -p forge-cli --release",
    "test:automation": "cargo test -p core --test automation_tests --test project_manifest_tests --test repair_tests --test video_tests && cargo test -p providers --tests && bash scripts/test-cli-product.sh && bash scripts/test-cli-installer.sh",
    "test:scripts": "node --check scripts/test-forge-dev-skill-source.mjs && node scripts/test-forge-dev-skill-source.mjs && bash -n install.sh scripts/test-cli-product.sh scripts/test-cli-installer.sh scripts/sign-dev-cli.sh scripts/test-cli-signing-contract.sh && bash scripts/test-cli-signing-contract.sh"
  }
}
